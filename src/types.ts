export interface UserData {
    nickname: string;
    name: string;
    surname: string;
    sex: string;
    country: string;
    region: string;
    myCar: string;
}

export interface UserBlog {
    text: string;
    title: string;
}